[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "cerebrl-bio"
version = "0.1.0"
description = "Biology and pharmacology applications for CerebRL"
requires-python = ">=3.11"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["src/cerebrl_bio"]

