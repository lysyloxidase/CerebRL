[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "cerebrl-bench"
version = "0.1.0"
description = "Benchmarks for CerebRL"
requires-python = ">=3.11"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["src/cerebrl_bench"]

